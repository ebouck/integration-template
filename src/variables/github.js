import { defineVariable } from "@bigidea/integration-connectors";

export const githubOwner = defineVariable({ name: "githubOwner" });
export const githubRepo = defineVariable({ name: "githubRepo" });
